export interface PGData {
    _id: string,
    ownerId: string,
    name: string,
    gender: string,
    price: number,
    roomType: string,
    location: string,
    fulladdress: string,
    available: boolean,
    images: string
}